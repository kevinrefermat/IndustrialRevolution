import XCTest
@testable import IndustrialRevolution

final class IndustrialRevolutionTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(IndustrialRevolution().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
